import 'package:flutter/material.dart';

class StockManeger extends StatelessWidget {
  const StockManeger({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blueAccent,
      ),
    );
  }
}
